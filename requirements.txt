# 도메인 로직(core/data/signals/scoring/reco)은 표준 라이브러리만으로 동작합니다.
# 아래는 웹 API·스케줄러·LLM 연동(I/O 계층)에 필요합니다.

fastapi>=0.110
uvicorn[standard]>=0.29

# LLM 근거 설명·뉴스 감성(선택; 없으면 오프라인 폴백으로 동작)
anthropic>=0.40

# 개발/테스트(선택; tests/run_tests.py 는 pytest 없이도 실행됨)
pytest>=8.0

# 실데이터 provider(KIS/DART/KRX/News) 구현 시 사용 예상(현재 스켈레톤):
# requests>=2.31
websocket-client>=1.7   # KIS 실시간 체결/호가 WebSocket (build002, live 모드 단타)
# pandas>=2.0
